<%= form_for @todo, remote: true, html: { data: { related: '#todo-list', related_action: 'prepend', related_clear: true } } do |f| %>
  <%= f.text_field :description, placeholder: 'What needs to be done?', id: 'new-todo', data: { autofocus: true } %>
<% end %>
