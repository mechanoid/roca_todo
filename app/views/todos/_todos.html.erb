<% filter_classes = {
        'completed' => 'active',
        'active'    => 'completed'
} %>

<% observe_todos = filter_classes[params[:filter]] %>
<ul id="todo-list" data-prepend-on="task.created"><%= render @todos %></ul>