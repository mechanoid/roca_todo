<% filter_classes = {
        'completed' => 'active',
        'active'    => 'completed'
} %>

<% observe_todos = filter_classes[params[:filter]] %>
<ul id="todo-list" data-observe="<%= ".#{observe_todos}" if observe_todos %>" data-observe-action="hide"><%= render @todos %></ul>